@model IEnumerable<NetC__CRUD.Models.Viatic>

@{
    ViewBag.Title = "Index";
}

<h2 style="text-align: center; font-weight: bold;">Viatic Records</h2>

<div>
    <a href="@Url.Action("Index","Home")" class="btn btn-primary">Inicio</a>
</div>
<button type="button" class="btn btn-secondary">
    @Html.ActionLink("Create New", "Create", null, new { @class = "text-white" })
</button>
<div class="table-responsive">
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.place)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.description1)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.amount)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.idEmployee)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.status)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.place)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.description1)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.amount)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.idEmployee)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.status)
                    </td>
                    <td>
                        <div class="btn-group" role="group">
                            <button type="button" class="btn btn-info">
                                @Html.ActionLink("Edit", "Edit", new { id = item.idViatic }, new { @class = "text-white" })
                            </button>
                            <button type="button" class="btn btn-warning">
                                @Html.ActionLink("Details", "Details", new { id = item.idViatic }, new { @class = "text-white" })
                            </button>
                            <button type="button" class="btn btn-danger">
                                @Html.ActionLink("Delete", "Delete", new { id = item.idViatic }, new { @class = "text-white" })
                            </button>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
