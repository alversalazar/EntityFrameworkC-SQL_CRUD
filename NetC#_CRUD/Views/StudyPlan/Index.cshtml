@model IEnumerable<NetC__CRUD.Models.StudyPlan>

@{
    ViewBag.Title = "Index";
}


<h2 style="text-align: center; font-weight: bold;">Study Plan Records</h2>

<div>
    <a href="@Url.Action("Index","Home")" class="btn btn-primary">Inicio</a>
</div>
<button type="button" class="btn btn-secondary">
    @Html.ActionLink("Create New", "Create", null, new { @class = "text-white" })
</button>
<div class="table-responsive">
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.objetive)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.starDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.finalDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.key1)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.status)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.idCareer)
                </th>
                <th></th>
            </tr>

        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.objetive)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.starDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.finalDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.key1)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.status)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.idCareer)
                    </td>
                    <td>
                        <div class="btn-group" role="group">
                            <button type="button" class="btn btn-info">
                                @Html.ActionLink("Edit", "Edit", new { id = item.idStudyPlan }, new { @class = "text-white" })
                            </button>
                            <button type="button" class="btn btn-warning">
                                @Html.ActionLink("Details", "Details", new { id = item.idStudyPlan }, new { @class = "text-white" })
                            </button>
                            <button type="button" class="btn btn-danger">
                                @Html.ActionLink("Delete", "Delete", new { id = item.idStudyPlan }, new { @class = "text-white" })
                            </button>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
